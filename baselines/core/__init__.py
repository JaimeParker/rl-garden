"""Shared infrastructure for baseline orchestrators: manifest loading, the
env-stepping wire protocol/bridge, dataset loading, evaluation helpers, and
result reporting. See ``baselines/baselines.yaml`` for the registered
baselines and ``.agents/runbooks/baseline-install.md`` for the workflow.
"""
