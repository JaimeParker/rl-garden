"""Training entrypoint implementations and algorithm registries."""

# FIXME(config-wiring): Args fields are still mapped manually into agent
# constructors. Any added or renamed field must update that wiring and its
# focused construction test until a safe typed mapping replaces it.
